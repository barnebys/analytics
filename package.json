{
  "babel": {
    "plugins": [
      "transform-inline-environment-variables"
    ],
    "presets": [
      [
        "@babel/preset-env",
        {
          "targets": "defaults, ie > 10"
        }
      ]
    ]
  },
  "devDependencies": {
    "@babel/core": "^7.7.7",
    "@babel/preset-env": "^7.7.6",
    "babel-plugin-transform-inline-environment-variables": "^0.4.3",
    "babelify": "^10.0.0",
    "browserify": "^16.5.0"
  },
  "scripts": {
    "build": "browserify -t babelify src/*.js --outfile public/*.js"
  },
  "dependencies": {
    "@google-cloud/bigquery": "^0.11.1",
    "emptygif": "^0.1.2",
    "encodeurl": "^1.0.2",
    "ip-anonymize": "^0.1.0",
    "micro-cookie-session": "^2.0.0-beta.1",
    "request-ip": "^2.1.3"
  }
}
